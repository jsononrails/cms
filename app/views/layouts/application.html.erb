<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Sault Sports Medicine<%= content_for?(:title) ? yield(:title) : "" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
	
	<!-- Top Nav -->
	<div class="fixed contain-to-grid custom-nav-bar">
		<nav class="top-bar" data-topbar> 
			<ul class="title-area large-offset-1 large-4 columns"> 
				<li class=""> <%= image_tag "logo_Small.png", class: "title-logo" %> </li> 
				<!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone --> 
				<li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li> 
			</ul> 
		
			<section class="top-bar-section"> 
			<!-- Right Nav Section --> 
			<ul class="right"> 
				<li class="active"><a href="#">About Us</a></li> 
				<li class="active"><a href="#">Services</a></li> 
				<li class="active"><a href="#">Team</a></li> 
				<li class="active"><a href="#">Contact</a></li> 
			</ul> 
			</section> 
		</nav>
	</div>
	<!-- End Top Nav -->
	
	<div class="row">
		<div class="large-12 columns orange-rule">
		</div>	
		<div class="large-12 columns banners">
			<% if @page.banners %>
			  <% @page.banners.each do |banner| %>
			    <%= image_tag(banner.url_content) %>
			  <% end %>
			<% end %>
			<div class="content large-offset-1 large-4 columns">
				banner text here
			</div>
		</div>
	</div>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>